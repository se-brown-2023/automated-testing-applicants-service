server:
  port: ${port:8000}
  shutdown: graceful

spring:
  application:
    name: exam-manage-service
  config:
    import: application-db.yml
  datasource:
    url:
    username:
    password:

graylog:
  server:

logging:
  file:
    name: ${log.dir:/tmp}/exam-manage-service.main.log